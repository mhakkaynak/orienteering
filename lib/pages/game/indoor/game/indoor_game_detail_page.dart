import 'package:flutter/material.dart';

class IndoorGameDetailPage extends StatelessWidget {
  const IndoorGameDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('TODO'),),
    );
  }
}